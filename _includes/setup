{% capture jbcache %}
  <!--
  - Dynamically set liquid variables for working with URLs/paths
  -->
  {% if site.alm.setup.provider == "custom" %}
    {% include custom/setup %}
  {% else %}
    {% if site.safe and site.alm.BASE_PATH and site.alm.BASE_PATH != '' %}
      {% assign BASE_PATH = site.alm.BASE_PATH %}
      {% assign HOME_PATH = site.alm.BASE_PATH %}
    {% else %}
      {% assign BASE_PATH = nil %}
      {% assign HOME_PATH = "/space" %}
    {% endif %}

    {% if site.alm.ASSET_PATH %}
      {% assign ASSET_PATH = site.alm.ASSET_PATH %}
    {% else %}
      {% capture ASSET_PATH %}{{ BASE_PATH }}/assets/themes/{{ page.theme.name }}{% endcapture %}
    {% endif %}  
  {% endif %}
{% endcapture %}{% assign jbcache = nil %}
